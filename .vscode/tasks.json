{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        // HelloWorld task
        {
            "label": "Build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-o", "build/HelloWorld", "-g", "mains.cpp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}